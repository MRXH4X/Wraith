# NOTE: replace /path/to/wraith (install dir), /home/USER (home), and USER=youruser with your values.
[Unit]
Description=Wraith token-free idle-path channel keepalive probe (prevents false watchdog respawns of an idle-but-healthy channels session)
After=wraith-channels.service

[Service]
Type=oneshot
ExecStart=/path/to/wraith/scripts/channel-keepalive-probe.sh
Environment=PATH=/home/USER/.local/bin:/home/USER/.bun/bin:/usr/local/bin:/usr/bin:/bin
Environment=HOME=/home/USER
Environment=TZ=Europe/Budapest
StandardOutput=append:/path/to/wraith/store/channel-keepalive-probe.log
StandardError=append:/path/to/wraith/store/channel-keepalive-probe.log
