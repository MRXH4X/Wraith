# NOTE: replace /path/to/wraith (install dir), /home/USER (home), and USER=youruser with your values.
[Unit]
Description=Wraith channels watchdog (coarse independent net for a wedged channels session)
After=wraith-channels.service

[Service]
Type=oneshot
ExecStart=/path/to/wraith/scripts/channel-watchdog.sh
Environment=PATH=/home/USER/.local/bin:/home/USER/.bun/bin:/usr/local/bin:/usr/bin:/bin
Environment=HOME=/home/USER
Environment=TZ=Europe/Budapest
StandardOutput=append:/path/to/wraith/store/channel-watchdog.log
StandardError=append:/path/to/wraith/store/channel-watchdog.log
